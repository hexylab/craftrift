# CraftRift: ボクセルFPS基盤 設計仕様書

## 概要

Minecraft風のボクセルワールドをWebブラウザ上で動作させる基盤を構築する。一人称視点でWASD移動、マウスで視点操作、ブロックの破壊・設置が可能。将来的にLeague of Legends（ARAM）のゲームシステムとマルチプレイヤー対戦を追加するための土台となる。

## スコープ

### 含まれるもの
- ボクセルワールドの描画（チャンクベース）
- WASD移動 + マウス視点操作（一人称視点）
- ブロックの破壊・設置
- ARAMレーン形状の固定マップ
- プレースホルダーテクスチャ（後から差し替え可能）

### 含まれないもの
- ジャンプ・重力
- マルチプレイヤー（WebSocket）
- ゲームシステム（タワー、ネクサス、HP、スキル）
- サウンド
- HUD（クロスヘア以外）

## 技術スタック

- **言語:** TypeScript
- **3Dライブラリ:** Three.js
- **ビルドツール:** Vite
- **パッケージマネージャ:** npm

## プロジェクト構成

```
craftrift/
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── public/
│   └── textures/
│       ├── grass_top.png
│       ├── grass_side.png
│       ├── dirt.png
│       ├── stone.png
│       └── bedrock.png
├── src/
│   ├── main.ts
│   ├── engine/
│   │   ├── Game.ts
│   │   ├── InputManager.ts
│   │   └── Renderer.ts
│   ├── world/
│   │   ├── World.ts
│   │   ├── Chunk.ts
│   │   ├── Block.ts
│   │   └── MapData.ts
│   ├── player/
│   │   ├── Player.ts
│   │   ├── Camera.ts
│   │   └── BlockInteraction.ts
│   └── utils/
│       └── TextureLoader.ts
```

### 責務

- **engine/**: ゲームループ、入力処理、レンダラー管理
- **world/**: ボクセルワールドのデータ構造とメッシュ生成
- **player/**: プレイヤー状態、カメラ制御、ブロックインタラクション
- **utils/**: テクスチャロードなどの共通ユーティリティ

## ボクセルワールド設計

### チャンクシステム

- チャンクサイズ: 16×16×16ブロック
- データ構造: `Uint8Array(4096)` でブロックIDを保持
- インデックス計算: `x + z * 16 + y * 256`
- チャンクごとにメッシュを生成、ブロック変更時はそのチャンクのみ再構築

### メッシュ生成（Culled Meshing）

- 隣接ブロックに隠れている面は生成しない
- 各露出面に対して2つの三角形（1つの四角形）を生成
- `BufferGeometry` で直接構築（位置、法線、UV、インデックス）
- Greedy Meshingは初期段階では不要

### ブロックタイプ

| ID | 名前 | 説明 | 破壊可能 |
|----|------|------|----------|
| 0 | AIR | 空気（描画なし） | - |
| 1 | GRASS | 草（上面/側面/底面でテクスチャが異なる） | Yes |
| 2 | DIRT | 土 | Yes |
| 3 | STONE | 石 | Yes |
| 4 | BEDROCK | 岩盤（マップ境界） | No |

### テクスチャシステム

- テクスチャアトラス方式: 全テクスチャを1枚の画像にまとめる
- 個別テクスチャサイズ: 16×16ピクセル
- ブロックタイプごとに上面・側面・底面のUV座標をマッピング
- プレースホルダーは単色 + 簡易パターンのPNG
- `public/textures/` のファイルを差し替えるだけでテクスチャ変更可能

### 固定マップ（ARAMレーン）

- レーン: 中央にまっすぐ伸びる道（幅約11ブロック、長さ約80ブロック）
- 拠点エリア: 両端にチーム拠点用スペース（将来のタワー・ネクサス配置用）
- 壁: レーン外側をBEDROCKの壁で囲む（落下防止）
- 地面: GRASS、壁: STONE、基盤: BEDROCK

## プレイヤーとカメラ設計

### 一人称カメラ

- Pointer Lock API でマウスカーソルをキャプチャ（クリックで有効化、Escで解除）
- マウス移動で視点回転（yaw/pitch）
- pitch制限: -89° ~ +89°
- カメラ位置 = プレイヤー位置 + 目の高さ（1.6ブロック）

### 移動

- W/S: カメラの向き方向に前進/後退
- A/D: カメラに対して左右に平行移動
- 移動速度: 一定（加速・慣性なし）
- 重力なし（クリエイティブモード的に空中も自由移動）

### ブロックインタラクション

- カメラ中心からレイキャストで正面のブロックを検出
- 最大距離: 5ブロック
- 左クリック: ターゲットブロックを破壊（AIRに変更）。BEDROCKは破壊不可
- 右クリック: ターゲットブロックの隣接面に新ブロックを設置（STONE）
- ハイライト: ターゲットブロックにワイヤーフレーム枠を表示

### HUD

- 画面中央にクロスヘア（+マーク）のみ

## ゲームループとレンダリング

### ゲームループ

- `requestAnimationFrame` ベース
- `deltaTime` 計算でフレームレート非依存の移動速度
- 処理順序: 入力処理 → プレイヤー更新 → ワールド更新 → レンダリング

### レンダラー設定

- Three.js `WebGLRenderer`
- FOV: 75°
- 描画距離: 128ブロック（固定マップに十分）
- ウィンドウリサイズ時に自動調整

### パフォーマンス方針

- `BufferGeometry` で直接メッシュ構築
- テクスチャアトラスで1マテリアル → ドローコール最小化
- ブロック変更時は該当チャンクのみ再構築（隣接チャンク境界面も考慮）
- Three.jsのフラストラムカリングをそのまま活用

## 将来の拡張ポイント

- ジャンプ・重力の追加
- マルチプレイヤー（WebSocketサーバー）
- ARAMゲームシステム（タワー、ネクサス、ミニオン、HP、スキル）
- チャンピオン選択・リスポーン
- HUD拡張（HP、スキルUI、ミニマップ）
- サウンドシステム
- Greedy Meshingによるパフォーマンス最適化
